#include <stdio.h>

/**
 *main - This function will jump some alphabets
 *
 *Return: It will return small letters
 */
int main(void)
{
	char letter = 'a';

	while (letter <= 'z')
	{
		if (letter != 'q' && letter != 'e')
		{
			putchar(letter);
		}
		letter++;
	}
	putchar('\n');
	return (0);
}
